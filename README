# Create database wice_grid_examples_development && check your database.yml

gem install erubis

./script/plugin install git://github.com/leikind/wice_grid.git

rake db:migrate
rake wg:populate

./script/generate wice_grid_assets

./script/server

OR:
 go to http://grid.leikind.org to view the examples online
 