<%= grid(@tasks_grid, :upper_pagination_panel => true) do |g|

  g.column :column_name => 'ID' do |task|
    task.id
  end

  g.column :column_name => 'Title'  do |task|
    task.title
  end

  g.column  :column_name => 'Description' do |task|
    task.description
  end

  g.column  :column_name => 'Archived' do |task|
    task.archived? ? 'Yes' : 'No'
  end

  g.column :column_name => 'Added' do |task|
    task.created_at.to_s(:short)
  end


  g.column   do |task|
    link_to('Edit', edit_task_path(task))
  end
end -%>
